<% if stage.present? && !stage.finished? %>
	<%= button_to('Generate '+stage.name+' results', stages_auto_generate_path(), method: :get, class: "btn btn-info mr-3", params: { id: stage.id }) %>
<% end %>
